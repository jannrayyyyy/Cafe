/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com', 'cdn.britannica.com', 'lh3.googleusercontent.com'],
    },
};

module.exports = nextConfig;
